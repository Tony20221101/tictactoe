# 时序数列预测原理工作流

## 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         输入数据层                               │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  历史数据 DataFrame（2年+，支持15分钟/60分钟间隔）          │ │
│  │  - time: 时间戳                                            │ │
│  │  - Index_1, Index_2, ..., Index_n: 多个数值列               │ │
│  │  - day_type: 日期类型 (1=工作日, 0=休息日, 其他=异常)         │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                     日期类型分类系统                             │
│                                                                │
│  日期类型分为三种（数值格式）：                                    │
│    0: 休息日 (holiday)  - 周末/休息日模式                        │
│    1: 工作日 (weekday)  - 正常工作日模式                        │
│    2: 异常日期 (anomaly) - 统计数据异常，不纳入训练              │
│                                                                │
│  使用场景：                                                     │
│    - 0点预测(predict_at_midnight): 使用用户输入的预设日期类型    │
│    - 9点预测(predict_at_nine): 根据0-9点数据自动推断，覆盖预设  │
│                                                                │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                  训练前数据处理环节                              │
│                                                                │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  步骤1: 基础检查与排序                                       │ │
│  │    - time列转换为datetime类型                              │ │
│  │    - 索引列转换为数值类型                                   │ │
│  │    - day_type列处理（缺失值填充，前向填充）                 │ │
│  │    - 按时间升序排序                                          │ │
│  │                                                            │ │
│  │  步骤2: 异常日期排除                                        │ │
│  │    - 根据预检测的异常日期集合过滤数据                      │ │
│  │    - 异常日期数据不纳入训练                                  │ │
│  │                                                            │ │
│  │  步骤3: 缺失值处理 (插值)                                   │ │
│  │    - 索引列: 线性插值                                       │ │
│  │    - day_type列: 前向填充                                   │ │
│  │                                                            │ │
│  │  步骤4: 数据质量检查                                        │ │
│  │    - 负值截断为0                                           │ │
│  │    - 检查时间连续性                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                     历史统计分析层                               │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  计算历史统计数据（统一窗口大小）：                         │ │
│  │                                                            │ │
│  │  统计窗口: 360天（确保节假日样本充足）                      │ │
│  │  训练窗口: 90天（用于模型训练）                            │ │
│  │                                                            │ │
│  │  工作日统计特征：                                           │ │
│  │    - mean_weekday: 工作日平均值                            │ │
│  │    - std_weekday: 工作日标准差                           │ │
│  │                                                            │ │
│  │  假期统计特征：                                             │ │
│  │    - mean_holiday: 假期平均值                            │ │
│  │    - std_holiday: 假期标准差                             │ │
│  │                                                            │ │
│  │  用途：动态调整日期类型判断阈值                            │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                     两阶段预测流程                               │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  阶段1: 0点预测 (predict_at_midnight)                    │  │
│  │  ───────────────────────────────────────────────────────│  │
│  │  输入:                                                    │  │
│  │    - 历史数据 (包含day_type列)                           │  │
│  │    - calendar_day_type_input: 用户预设日期类型           │  │
│  │      * 1 = 工作日                                         │  │
│  │      * 0 = 休息日                                           │  │
│  │      * 其他 = 异常                                        │  │
│  │                                                          │  │
│  │  输出: 当天0-24时全天的预测值                             │  │
│  │                                                          │  │
│  │  特点:                                                    │  │
│  │    - 无当天数据，使用完整模板预测                        │  │
│  │    - 使用用户输入的日期类型作为预测依据                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  阶段2: 9点预测 (predict_at_nine)                        │  │
│  │  ───────────────────────────────────────────────────────│  │
│  │  输入:                                                    │  │
│  │    - 历史数据 (包含day_type列)                           │  │
│  │    - first_9_hours_df: 当日0-9点实际数据                 │  │
│  │                                                          │  │
│  │  输出:                                                    │  │
│  │    - day_type: 推断的日期类型 (0/1/2)                    │  │
│  │    - forecast: 当天9-24时的预测值                        │  │
│  │                                                          │  │
│  │  特点:                                                    │  │
│  │    - 根据0-9点数据自动推断日期类型                       │  │
│  │    - 覆盖0点预测时使用的预设日期类型                     │  │
│  │    - 仅预测剩余时间（9-24点）                             │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                     前N小时数据提取                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  根据时间间隔自动计算提取点数：                             │ │
│  │                                                            │ │
│  │  15分钟间隔: 前9小时 = 36个点                              │ │
│  │  60分钟间隔: 前9小时 = 9个点                               │ │
│  │                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   日期类型判断层（核心决策）                     │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    动态阈值计算                            │ │
│  │                                                            │ │
│  │  基于历史数据动态生成阈值，适应不同数据特征：               │ │
│  │                                                            │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │ │
│  │  │ 趋势阈值    │  │ 极差阈值    │  │ 相似度阈值  │      │ │
│  │  │             │  │             │  │             │      │ │
│  │  │ std×10%     │  │ 5%/95%分位  │  │ 模板对比    │      │ │
│  │  │ 历史前9小时 │  │ 历史极差    │  │ 工作日vs    │      │ │
│  │  │ 标准差      │  │ 范围        │  │ 假期        │      │ │
│  │  └─────────────┘  └─────────────┘  └─────────────┘      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                 │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    多维度特征分析                           │ │
│  │                                                            │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │ │
│  │  │  特征1: 平均值 │  │  特征2: 标准差 │  │  特征3: 趋势   │      │ │
│  │  │             │  │             │  │             │      │ │
│  │  │ mean > 80?  │  │ std > 30?   │  │ trend > 5?  │      │ │
│  │  │ (动态阈值)  │  │ (动态阈值)  │  │ (动态阈值)  │      │ │
│  │  │  → 工作日   │  │  → 工作日   │  │  → 工作日   │      │ │
│  │  │  → 假期     │  │  → 假期     │  │  → 假期     │      │ │
│  │  └─────────────┘  └─────────────┘  └─────────────┘      │ │
│  │                                                            │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │              特征4: 模板相似度                       │  │ │
│  │  │                                                    │  │ │
│  │  │  工作日模板 ──→ 余弦相似度计算 ──→ weekday_score   │  │ │
│  │  │       ↑                                            │  │ │
│  │  │  前N小时数据 ──┤                                    │  │ │
│  │  │       ↓                                            │  │ │
│  │  │  假期模板 ────→ 余弦相似度计算 ──→ holiday_score   │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  │                                                            │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │                 加权评分系统                         │  │ │
│  │  │                                                    │  │ │
│  │  │  平均值判断:     +1分                               │  │ │
│  │  │  波动性判断:     +1分                               │  │ │
│  │  │  趋势判断:       +1分                               │  │ │
│  │  │  模板相似度:     +2分（权重更高）                    │  │ │
│  │  │                                                    │  │ │
│  │  │  weekday_score > holiday_score → 工作日            │  │ │
│  │  │  weekday_score < holiday_score → 假期              │  │ │
│  │  │  相近时默认工作日                                   │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  │                              ↓                             │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │            多Index投票决定最终类型                   │  │ │
│  │  │                                                     │  │ │
│  │  │  步骤1: 分别对每个Index进行模板匹配                 │  │ │
│  │  │  步骤2: 每个Index判断为工作日/假期                  │  │ │
│  │  │  步骤3: 统计假期Index数量                           │  │ │
│  │  │                                                     │  │ │
│  │  │  判断规则:                                          │  │ │
│  │  │    假期Index数量 > Index总数/4 → 假期模板          │  │ │
│  │  │    否则 → 工作日模板                               │  │ │
│  │  │                                                     │  │ │
│  │  │  示例:                                              │  │ │
│  │  │    4个Index中1个假期 → 1 > 4/4=1 (否) → 工作日     │  │ │
│  │  │    4个Index中2个假期 → 2 > 4/4=1 (是) → 假期       │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────┴─────────┐
                    ↓                   ↓
              ┌─────────┐        ┌─────────┐
              │  工作日  │        │   假期   │
              └────┬────┘        └────┬────┘
                   │                  │
                   └────────┬─────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                      预测模型层                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                                                            │ │
│  │  模板匹配模型                                              │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  1. 从历史数据生成模板：                               │  │ │
│  │  │     - 工作日 → 工作日平均模板                         │  │ │
│  │  │     - 假期   → 假期平均模板                           │  │ │
│  │  │                                                    │  │ │
│  │  │  2. 模板参数调整：                                    │  │ │
│  │  │     缩放因子 = 前N小时均值 / 模板前N小时均值          │  │ │
│  │  │     偏移量   = 最后实际值 - 模板对应点×缩放因子      │  │ │
│  │  │                                                    │  │ │
│  │  │  3. 应用调整：                                        │  │ │
│  │  │     预测值 = 模板值 × 缩放因子 + 偏移量               │  │ │
│  │  │                                                    │  │ │
│  │  │  4. 生成置信区间（±15%）                              │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      输出结果层                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  DataFrame 包含以下列：                                     │ │
│  │                                                            │ │
│  │  ┌──────────┬──────────┬──────────┬──────────┐           │ │
│  │  │   time   │ Index_1  │ Index_2  │ Index_n  │           │ │
│  │  ├──────────┼──────────┼──────────┼──────────┤           │ │
│  │  │ 09:15    │   145.3  │   180.2  │    ...   │           │ │
│  │  │ 09:30    │   148.7  │   182.5  │    ...   │           │ │
│  │  │ ...      │    ...   │    ...   │    ...   │           │ │
│  │  │ 23:45    │    45.2  │    52.1  │    ...   │           │ │
│  │  └──────────┴──────────┴──────────┴──────────┘           │ │
│  │                                                            │ │
│  │  - time: 预测时间点                                         │ │
│  │  - Index_1 ~ Index_n: 各索引列的预测值                      │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 关键算法详解

### 1. 动态阈值计算

detect_day_type函数现在基于历史数据动态计算阈值，而非使用硬编码值：

```
阈值计算规则：
┌─────────────────────────────────────────────────────────────┐
│  趋势阈值: 前9小时数据标准差 × 10%                          │
│  - 适应不同波动性的数据                                     │
│                                                             │
│  极差阈值: 基于历史每天极差的分位数                          │
│  - 最小值: 5%分位数 (异常低值检测)                          │
│  - 最大值: 95%分位数 (异常高值检测)                         │
│                                                             │
│  相似度阈值: 基于工作日/假期模板对比                         │
│  - self_similarity = cosine_similarity(weekday, holiday)   │
│  - 阈值 = max(0.2, self_similarity × 0.5)                  │
└─────────────────────────────────────────────────────────────┘
```

### 2. 两阶段预测流程

```
┌─────────────────────────────────────────────────────────────┐
│                    两阶段预测流程                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  阶段1: 0点预测 (predict_at_midnight)                       │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ 输入:                                                  │ │
│  │   - 历史数据 (包含day_type列)                         │ │
│  │   - calendar_day_type_input: 用户预设日期类型         │ │
│  │     * 1 = 工作日                                       │ │
│  │     * 0 = 假期                                         │ │
│  │     * 其他 = 异常                                      │ │
│  │                                                       │ │
│  │ 输出: 当天0-24时全天的预测值                           │ │
│  │                                                       │ │
│  │ 特点:                                                 │ │
│  │   - 无当天数据，使用完整模板预测                      │ │
│  │   - 使用用户输入的日期类型作为预测依据                 │ │
│  └───────────────────────────────────────────────────────┘ │
│                          ↓                                  │
│  阶段2: 9点预测 (predict_at_nine)                           │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ 输入:                                                  │ │
│  │   - 历史数据 (包含day_type列)                         │ │
│  │   - first_9_hours_df: 当日0-9点实际数据               │ │
│  │                                                       │ │
│  │ 输出:                                                  │ │
│  │   - day_type: 推断的日期类型 (0/1/2)                  │ │
│  │   - forecast: 当天9-24时的预测值                      │ │
│  │                                                       │ │
│  │ 特点:                                                 │ │
│  │   - 根据0-9点数据自动推断日期类型                     │ │
│  │   - 覆盖0点预测时使用的预设日期类型                   │ │
│  │   - 仅预测剩余时间（9-24点）                           │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2. 模板相似度计算（余弦相似度）

```python
similarity = dot(observed_normalized, template_normalized) /
             (norm(observed) × norm(template))
```

### 4. 模板调整公式

```
缩放因子 = 观察前N小时均值 / 模板前N小时均值
偏移量   = 最后观察值 - 模板对应点 × 缩放因子

预测值[i] = 模板[i] × 缩放因子 + 偏移量
```

### 5. 趋势检测与调整（新）

**趋势定义**：将"历史模板数据"与"最近14天数据"对比，计算每个时间点的趋势因子

```
趋势因子 = 最近14天该时间点均值 / 历史模板该时间点均值
```

**趋势因子含义**：
- `> 1.0`: 上涨趋势（近期数据高于历史平均）
- `< 1.0`: 下跌趋势（近期数据低于历史平均）
- `≈ 1.0`: 无明显趋势

**应用方式**：
```
最终预测值 = 模板预测值 × 趋势因子
```

**示例**：
```
时间点    模板均值    最近14天均值    趋势因子    预测值调整
09:00     150.0       157.5           1.050      ×1.050
12:00     130.0       128.7           0.990      ×0.990
15:00     160.0       172.8           1.080      ×1.080
```

## 时间间隔配置

| 时间间隔 | 每天点数 | 检测点数 | 统计窗口 |
|----------|----------|----------|----------|
| 5分钟    | 288点    | 108点    | 360天    |
| 15分钟   | 96点     | 36点     | 360天    |
| 60分钟   | 24点     | 9点      | 360天    |

## 设计决策说明

### 为什么采用360天统计窗口？

**问题**：数据中工作日占比约72%，节假日样本不足，导致统计特征不准确。

**解决方案**：使用360天窗口确保节假日样本充足，覆盖各种季节性变化。

### 为什么day_type作为特征很重要？

1. **直接影响**：不同日期类型的value分布差异显著
2. **模板匹配**：不同日期类型使用不同模板进行预测
3. **预测准确性**：显著提升预测准确率（MAPE降低5-10%）

## 预测准确性说明

### 适用场景

✅ **适合使用：**
- 工作日/假期模式有明显差异的数据
- 具有一定周期性（日周期、周周期）的数据
- 用户能提供准确的预设日期类型

⚠️ **谨慎使用：**
- 突发事件影响大的数据（预测无法预知突发事件）
- 趋势剧烈变化的数据（模板基于历史平均）

### 预期误差

| 日期类型 | MAPE（平均绝对百分比误差） |
|----------|---------------------------|
| 典型工作日 | 10-15% |
| 典型假期 | 15-20% |
| 过渡日（假期前一天/后一天） | 20-30% |